#main, test, preview or local [case insensitive]
ENVIRONMENT=test
ACCOUNT_ID=
PRIVATE_KEY=

#DEPLOYMENT
# LAZY_BURN_PERCENT=50
# LAZY_DELEGATE_REGISTRY_CONTRACT_ID=0.0.7221486
# LSH_GEN1_TOKEN_ID=0.0.768753
# LSH_GEN2_TOKEN_ID=0.0.848553
# LSH_GEN1_MUTANT_TOKEN_ID=0.0.1693200
# LAZY_GAS_STATION_CONTRACT_ID=0.0.7221483
# LAZY_TOKEN_ID=0.0.1311037
# LAZY_DECIMALS=1
# LAZY_SCT_CONTRACT_ID=0.0.1311003
# PRNG_CONTRACT_ID=0.0.8257116
## USE ECDSA KEY FOR SIGNING
# SIGNING_KEY=

# in whole units of $LAZY
INITIAL_LOTTO_JACKPOT=2000
LOTTO_LOSS_INCREMENT=50

# SOURCE VERIFICATION (Sourcify via @lazysuperheroes/hedera-verify)
# Set VERIFY_ON_DEPLOY=true to auto-verify each contract on the public Sourcify
# (sourcify.dev) right after it is deployed. Read-only: no key, no gas. This
# PUBLISHES the deployed source code, so only enable it for contracts you intend
# to verify publicly. Leave unset/false to deploy without verifying; you can
# verify any time afterwards with `npx hedera-verify harness`.
# VERIFY_ON_DEPLOY=true

# POST DEPLOYMENT
#LAZY_TRADE_LOTTO_CONTRACT_ID=

#LAZY_LOTTO_STORAGE=
#LAZY_LOTTO_CONTRACT_ID=
#LAZY_LOTTO_POOL_MANAGER_ID=

# MULTI-SIGNATURE CONFIGURATION
# Optional: Only needed if using multi-sig for admin operations
# See docs/MULTISIG_USER_GUIDE.md for details

# Multi-sig signers (comma-separated account IDs for reference only)
# Example: MULTISIG_SIGNERS=0.0.123456,0.0.234567,0.0.345678
#MULTISIG_SIGNERS=

# Multi-sig threshold (M of N required signatures)
# Example: MULTISIG_THRESHOLD=2 (requires 2 signatures)
#MULTISIG_THRESHOLD=

# Multi-sig workflow mode (interactive or offline)
# interactive: Real-time signing with <110s timeout
# offline: Asynchronous signing with no timeout
#MULTISIG_WORKFLOW=interactive

# Multi-sig export directory (for offline workflow)
#MULTISIG_EXPORT_DIR=./multisig-transactions

# Multi-sig audit log path
#MULTISIG_AUDIT_LOG=./logs/audit.log

# SECURITY WARNING:
# - Never store private keys in .env (use encrypted files or prompts)
# - Use PRIVATE_KEY above only for single-sig operations
# - For multi-sig, use one of these methods:
#   1. Prompt input (highest security): No env vars needed
#   2. Encrypted files: Create with `node lib/multiSig/cli/createKeyFile.js`
#   3. Environment vars (dev/test only): Not recommended for production