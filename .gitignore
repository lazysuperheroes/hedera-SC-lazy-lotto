node_modules
.env
coverage
coverage.json
typechain
env
.env.old
typechain-types

#Hardhat files
cache
artifacts
yarn.lock
logs/*
.DS_Store
*.key
# Auto-generated documentation (HTML/JS from hardhat-docgen)
docs/*
# But include hand-written markdown documentation
!docs/*.md
!docs/design-proposals/
!docs/design-proposals/*.md

# Claude Code frameworks (exclude all)
.claude/
.vscode/

*-cwd

# Temporary deployment state files
scripts/deployments/deployment-*.json
scripts/deployments/generateExtraPrizes.js
deployment-state.json

# Prize batch files and old contract data (contain serials, amounts, contract-specific data)
prizes-*.json
old_contract_prizes.json
all_prizes.json
extracted-prizes/

# Internal strategy documents
POOL_STRATEGY.md
SOFT_LAUNCH_PRIZE_PLAN.md

# Internal operational prize recipes — their _notes name treasury wallets
# (admin hub, gem vault), held-back reserve inventory, and sourcing strategy.
# Keep local; only the sanitized example templates are tracked.
scripts/interactions/LazyLotto/admin/recipes/*.json
!scripts/interactions/LazyLotto/admin/recipes/lazyLounge-stage1.json
!scripts/interactions/LazyLotto/admin/recipes/luckyDip-initial.json